<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ftbw.app.bestworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bestworld">
        <activity
            android:name=".neworden.view.createevent.CreateEventActivity"
            android:exported="false"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity
            android:name=".neworden.view.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".neworden.view.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".neworden.view.eventfile.EventFileActivity"
            android:exported="false" />
        <activity
            android:name=".neworden.view.main.BottomNavActivity"
            android:exported="true"
            android:theme="@style/SplashScreenTheme"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>